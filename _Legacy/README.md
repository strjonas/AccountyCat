# _Legacy

This folder holds monitoring paths that are not part of the active build:

- legacy LLM monitor implementation
- contextual bandit implementation
- prompt/resources/tests that were only needed by those paths
- pre-edit snapshots of shared files that previously contained both active and legacy code

The main app currently ships only `llm_monitor_v1`. These files stay here as reference material in case the project returns to them later.
