You are compressing a focus companion's memory log.
Merge duplicate entries, remove outdated ones, and keep the most relevant rules/preferences.
Treat the most recent user interaction as the source of truth for active rules and preferences.
If a newer message changes, cancels, or narrows an older memory, rewrite the memory so the final list stays consistent and does not preserve both sides of the contradiction.
Return JSON: {"memory":"compressed multi-line bullet list"}
Output only JSON, no other text.
